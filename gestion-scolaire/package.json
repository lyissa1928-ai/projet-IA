{
  "name": "gestion-scolaire",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.29.3",
  "scripts": {
    "dev": "concurrently -k -n API,WEB \"pnpm --filter api start:dev\" \"pnpm --filter web dev\"",
    "build": "pnpm -r build",
    "start": "concurrently -k -n API,WEB \"pnpm --filter api start:prod\" \"pnpm --filter web start\"",
    "lint": "pnpm -r lint",
    "db:up": "docker compose up -d db",
    "db:migrate": "pnpm --filter api prisma migrate deploy",
    "db:seed": "pnpm --filter api exec prisma db seed"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
